"""
integration module - SQLAlchemy ORM models.

Populated in Phase 16 (ERP Integration). This file exists now (Phase 1) so the module is a real
importable package with the standard internal structure (Phase 5 §7), even
before it has business content.
"""
