"""
question_bank module - DB access layer.

Populated in Phase 10 (Question Bank). This file exists now (Phase 1) so the module is a real
importable package with the standard internal structure (Phase 5 §7), even
before it has business content.
"""
